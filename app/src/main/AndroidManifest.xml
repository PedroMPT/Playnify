<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ismai.pedro.sisproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ScheduleGameActivity" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Activities.EditPasswordActivity" android:theme="@style/AppTheme.Dark"/>
        <activity android:name=".Activities.EditEmailActivity" android:theme="@style/AppTheme.Dark"/>
        <activity android:name=".Activities.EditNameActivity" android:theme="@style/AppTheme.Dark"/>
        <activity android:name=".Activities.EditProfileActivity" android:theme="@style/AppTheme.Dark"/>
        <activity
            android:name=".Activities.ProfileActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ForgotPasswordActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Activities.SignUpActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/AppTheme.Dark" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>